version: 1.0
runtime: python3

build:
  commands:
    build:
      - pip install fastapi
      - pip install "uvicorn[standard]"

run: 
  command: uvicorn main:app --port 3000
  network:
    port: 3000